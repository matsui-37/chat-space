.bottom
  - current_user.groups.each do |group|
    .bottom__group
      = link_to group_chats_path(group) do
        .bottom__group--name
          = group.name
        .bottom__group--message
          へえけえっげげ
